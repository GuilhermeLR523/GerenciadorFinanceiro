<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="gerenciadorFinanceiroPU">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>br.com.cotemig.gerenciadorfinanceiro.model.Conta</class>
        <class>br.com.cotemig.gerenciadorfinanceiro.model.Categoria</class>
        <class>br.com.cotemig.gerenciadorfinanceiro.model.Transacao</class>
        <class>br.com.cotemig.gerenciadorfinanceiro.model.Meta</class>

        <properties>

            <!-- Conexão com SQLite -->
            <property name="jakarta.persistence.jdbc.driver"
                      value="org.sqlite.JDBC"/>


            <!-- Dialeto SQLite -->
            <property name="hibernate.dialect"
                      value="org.hibernate.community.dialect.SQLiteDialect"/>

            <!-- Criação/atualização das tabelas -->
            <property name="hibernate.hbm2ddl.auto"
                      value="update"/>

            <!-- Exibe o SQL no console -->
            <property name="hibernate.show_sql"
                      value="true"/>

            <property name="hibernate.format_sql"
                      value="true"/>

        </properties>

    </persistence-unit>

</persistence>